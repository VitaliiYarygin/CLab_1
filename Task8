#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    double a = 1, b = 12.21, c;
    
   c = (0.81 * pow (a, 1/3) - 1/2.125 * pow (b, 1/3)) * exp(a);
   cout << c;
    
    return 0;
}
