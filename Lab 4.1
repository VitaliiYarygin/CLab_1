#include <cstdlib>
#include <iostream>
#include <cmath>
 
using namespace std;
 
int main() {
    float x, y;
    int N = 6;
    
    for (int i=1; i<N; i++ ){
    
    cout << "\n\nEnter a number x: ";
    cin >> x;
    
    y = pow(sin(x), 5) + abs(5*x-1.5);
    
    cout << "Result is: ";
    cout << y;
    }
    
    return 0;
    
}
