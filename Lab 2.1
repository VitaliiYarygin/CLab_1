#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    float x;
    float y;
    
    cout << "Enter a value x: ";
    cin >> x; 
    
    y = exp(-x+2); 
    
    if (x >= 0) {
        cout << "\n1. Result is: " << y; 
        } 
        else {
            cout << "\n1. x lower than or not equal 0";
        }
        
    y = exp(1/x) * sin(1/(-x+3.2));
    
        if (x < 0) {
        cout << "\n2. Result is: " << y; 
        } 
        else {
            cout << "\n2. x greater than 0";
 
     return 0;
        }
}
