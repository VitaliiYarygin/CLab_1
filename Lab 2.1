#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    int x;
    double y;
    
    y = exp((1/x)+2); 
    if (x >= 0) {
        cout << "1. x greater than or equal to 0"; 
        } 
        else {
        cout << "1. x lower than 0";
        }
    
    y = exp(1/x) * sin(1/(x+3,2));
    if (x >= 0) {
        cout << "\n2. x greater than or equal to 0"; 
        } 
        else {
        cout << "\n2. x lower than 0 ";
        }
    
     return 0;
}
